[package]
name = "agentos-config"
version = "0.1.0"
edition = "2021"
description = "Model and provider configuration for AgentOS."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
