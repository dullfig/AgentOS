[package]
name = "agentos-bitnet"
version = "0.1.0"
edition = "2021"
description = "Pure-Rust 1.58-bit inference engine — ternary matmul, LUT kernels, zero multiplication."

[dependencies]
thiserror = "2"
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
